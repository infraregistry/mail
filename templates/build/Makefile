# build:
# 	for f in $(shell find src -name "*.mjml"); do \
# 		x=$(basename $$f); \
# 		echo $$x; \
# 		npx mjml -o ../dist/$$x.html $$f; \
# 	done
build:
	npx mjml -o ../dist/registration.html src/registration.mjml